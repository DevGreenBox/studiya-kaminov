# Студия каминов — состояние проекта

**Обновлено:** 2026-08-24
**Этап по протоколу `clode.md`:** Design Execution → Documentation / Handoff
**Этап по ТЗ `Electric_Fireplaces.md`:** пройдены этапы 1–14 (материалы → финальный polish)

---

## 1. Где что лежит

| Что                     | Адрес                                                   |
| ----------------------- | ------------------------------------------------------- |
| Боевой сайт             | **https://studiya-kaminov.vercel.app**                  |
| Панель Vercel           | https://vercel.com/devgreenbox/studiya-kaminov          |
| Репозиторий             | https://github.com/DevGreenBox/studiya-kaminov (public) |
| Проект локально         | `/home/coder/iuiulee/Electric_Fireplaces`               |
| Прежний адрес (мёртвый) | https://studiya-kaminov.netlify.app — устаревшая версия |

Аккаунты: GitHub `leykalee` (организация `DevGreenBox`), Vercel `leykalee`
(команда `devgreenbox`) — оба CLI авторизованы в этой среде. Netlify
`sasha.gordon5621@gmail.com` — больше не используется.

---

## 2. Хостинг — переехали с Netlify на Vercel

На Netlify кончились кредиты бесплатного тарифа, и он начал отклонять любую
сборку: `Skipped due to account credit usage exceeded`. Блокировались оба пути —
и автосборка из GitHub, и выкладка локальной сборки. Ограничение аккаунта, не
проекта, поэтому чинилось только деньгами или ожиданием 11.09.2026.

Заказчик выбрал переезд на Vercel — это разработчик Next.js, бесплатный тариф
щедрее, плагин-адаптер не нужен.

**Что изменилось в коде:** заголовки безопасности и кэш для `/brand` и
`/images` переехали из `netlify.toml` в `next.config.ts`. Это важнее, чем
кажется: в конфиге хостинга они бы потерялись при переезде молча, без единой
ошибки в сборке. Сам `netlify.toml` и `@netlify/plugin-nextjs` удалены —
Vercel определяет Next.js без конфига.

**Автодеплой подключён:** `git push` в `main` сам пересобирает сайт.
Подключение делается только через веб-интерфейс — из CLI `vercel git connect`
возвращает `Failed to connect`, пока приложение Vercel не установлено в
GitHub-аккаунт. Инструкция в `DEPLOY.md`, раздел «Вариант 2».

**Старый сайт на Netlify** отвечает 200 и показывает устаревшую версию. Его
стоит удалить, чтобы не расходились две копии; я этого не делал без спроса.

---

## 3. Что сделано

Многостраничный магазин на Next.js 16 (App Router) + React 19 + TypeScript +
Tailwind CSS v4. Сборка проходит: 0 ошибок TypeScript, 0 ошибок ESLint,
**32 e2e-теста Playwright** зелёные на desktop и mobile.

Реализованы: главная с каруселью баннеров, каталог с фильтрами и сортировкой,
страница товара, поиск, избранное, корзина, промокоды, оформление заказа с
выбором перевозчика, накладная, письма, страницы «О нас», «Отзывы»,
«Оплата и доставка», «Контакты», «Политика», 404, sitemap и robots.

### Отклонения от протокола (зафиксированы осознанно)

**Интервью не проводилось в полном объёме.** `clode.md` требует структурированного
интервью до проектирования. Заказчик передал детальное ТЗ (`Electric_Fireplaces.md`,
81 раздел) и бриф, и команду начинать. Задано три уточняющих вопроса (цены, бренд,
конфликт названий Версаль/Верона) — ответы учтены.

**Видео огня в hero заменено CSS-анимацией.** ТЗ (п.13) просит видео. В материалах
его нет. Пламя на реальном фото горящего очага оживляется четырьмя CSS-слоями.
Слот под видео готов: `heroMedia.video` в `src/config/site.ts`.

---

## 4. Источники данных

**Материалы заказчика:** архив `Для сайта.rar` (183 МБ) по ссылке
`https://disk.yandex.ru/d/uPvtY3zkN94mQg`. Скачивается через публичный API
Яндекс.Диска, распаковывается `node-unrar-js` (RAR5). Внутри 200 файлов:
~140 фотографий и 15 `.docx` с описаниями.

**Бриф с брендом:** Google Docs
`https://docs.google.com/document/d/1RO7fDiWo0_dR4Ifqq2NqMz5BGXwygsnOd4XdwCrL55U`.
Экспортируется как `.docx`, логотип лежит в `word/media/image1.jpg`. Оттуда взяты
название «Студия каминов» и логотип (вырезан в прозрачный PNG).

> ⚠️ Временная папка со распакованными материалами (`scratchpad`) в этой среде
> периодически очищается. Чтобы пересобрать изображения с нуля, архив нужно
> скачать заново.

---

## 5. Каталог

18 позиций в 6 категориях, структура повторяет папки заказчика:

| Категория           | Модели                                              |
| ------------------- | --------------------------------------------------- |
| Камины с камнем     | Дублин (6 цветов), Мальта (5 цветов), тумбы-витрины |
| Классические камины | Верона, Версаль                                     |
| Современные камины  | Модерн                                              |
| С боковыми тумбами  | Дублин Премиум                                      |
| Угловые камины      | Мальта угловая                                      |
| Тумбы под ТВ        | Честер                                              |

Цветовые исполнения — отдельные товары, связанные полем `model`; на странице товара
показываются переключателем «Исполнение портала».

---

## 6. Дизайн-система

Токены — CSS-переменные в `src/app/globals.css` (`@theme`). HEX по компонентам
не разбросан.

**Направление — интерьерный editorial.** Фон не белый, а молочный; радиусы
2–10 px; теней почти нет; секции не завёрнуты в скруглённые плашки.
Композицию делает фотография и типографика, а не контейнер вокруг них.

| Токен                                | Значение              | Роль                             |
| ------------------------------------ | --------------------- | -------------------------------- |
| `--color-primary`                    | `#e97821`             | CTA, акценты, активные состояния |
| `--color-primary-hover` / `-active`  | `#d2650f` / `#b95709` | состояния кнопок                 |
| `--color-primary-soft`               | `#fdefe1`             | подложки акцентов                |
| `--color-brand-red` / `-grey`        | `#a23131` / `#9b9b9b` | цвета логотипа                   |
| `--color-canvas` / `--color-surface` | `#ffffff` / `#fbf8f4` | фон / чередующиеся секции        |
| `--color-ink` / `--color-ink-soft`   | `#1c1917` / `#57534e` | текст / вторичный текст          |
| `--color-line` / `-strong`           | `#e9e2d8` / `#d8cec0` | разделители / рамки полей        |

**Шрифты — пара.** `Golos Text` (Паратайп, кириллица родная) для интерфейса и
`Lora` для заголовков. Подключены через `next/font/google`, доступны как
`font-sans` и `font-display`. Масштаб задан утилитами `display-xl`,
`display-lg`, `display-md` и `eyebrow`: раньше все заголовки секций были одного
кегля, и страница читалась как список одинаковых блоков.

Потолок `display-xl` подобран по слову «Электрокамины» — оно должно помещаться
в колонку первого экрана целиком. Переносов по слогам в заголовках нет
намеренно: разорванное пополам название читается как опечатка.
Контейнер 1320 px, отступы 16/24/40 px.

Свои классы вместо системных контролов: `.ef-checkbox` (белая галочка на оранжевом,
системный `accent-color` рисовал тёмную), `.ef-select` (стрелка с отступом 12 px
от края), `.ef-radio`.

**Ритм секций.** Отступы блоков 40/48 px, подвал отделён 32 px. Раньше между
блоками набегало 145–160 px — заказчик назвал это пустыми полями. Причина была
не в секциях, а в `mt-24` у подвала поверх собственного отступа последней секции.

**Первый экран главной.** Тёмный, фотография уходит в правый край экрана.
Колонка со снимком тянется по высоте текстовой, поэтому её пропорция заранее
неизвестна — координаты живого огня считаются от реального прямоугольника
фотографии при `object-cover` (хук `useCoverBox`), иначе пламя уезжает мимо очага.

**Иконки — все «от руки».** Геометрия (lucide для интерфейса, свои контуры для
информационных блоков) прогоняется через rough.js скриптом
`scripts/make-sketch-icons.mjs` → `npm run icons`. В браузер уезжают готовые
пути, библиотека остаётся на этапе сборки. Подробности и параметры — в
`README.md`, раздел «Иконки». `lucide-react` переведена в devDependencies и
закреплена на 0.469: в 1.x структура пакета другая и разбор сломается.

---

## 7. Ключевые решения

**Типографика.** `src/lib/typography.ts` — функция `typo()` расставляет неразрывные
пробелы: короткие предлоги и союзы, число с единицей измерения, тире с предыдущим
словом. Применяется на уровне данных каталога и внутри переиспользуемых
компонентов. Проверено: ноль висячих предлогов на 12 страницах × 6 брейкпоинтах.

**Изображения.** Двухступенчатый конвейер:

1. `scripts/prepare-images.mjs <путь к архиву>` — отбор по `scripts/image-manifest.json`,
   конвертация в WebP, единые пропорции (товары 3:4);
2. `scripts/make-covers.mjs` — производные кадры из готовых WebP: обложки категорий
   (`COVERS`), баннеры карусели (`PROMOS`), кадры блока производства (`PRODUCTION`)
   и главные фото карточек (`CARDS`). Второй скрипт работает без исходного архива.

У четырёх моделей (Честер, Мальта угловая, Верона, Мальта венге) среди исходников
нет ни одного кадра без маркетплейс-надписей — для них `CARDS` вырезает середину
кадра. Границы текстовых полос выверены по каждому фото отдельно.

**Доставка.** Два перевозчика в `src/config/site.ts` → `carriers`: СДЭК (`primary`)
и «Деловые Линии». API считает обе разом, покупатель выбирает при оформлении.
Провайдеры: `mock-provider` (демо-формула), `cdek-provider`, `dellin-provider` —
подключаются независимо через переменные окружения.

**Хранилище заказов.** `src/lib/orders/store.ts` — интерфейс `OrderStore` с
реализацией в памяти. На serverless история не накапливается (функции не делят
память): заказ создаётся, письма уходят, накладная показывается, но выдать старый
заказ по номеру нельзя. Под историю нужен класс с `save`/`find` в `getOrderStore()`.

---

## 8. Данные, требующие подтверждения заказчиком

**Фотографии.** Заказчик прислал чистые кадры для четырёх моделей — «Дублин
Премиум», «Дублин слоновая кость», «Модерн», «Честер»; они лежат в
`incoming/products` и подменяют архивные. Остальным моделям такие снимки ещё
нужны: у части единственный кадр без маркетплейс-надписей низкого разрешения
(«Дублин шимо» 650×866), у «Мальты угловой», «Вероны» и «Мальты венге» главное
фото собирается обрезкой в обход надписей и получается 500–800 px.

Всё перечислено в README, раздел «Данные, которые требуют подтверждения». Кратко:

- **Цены** — прайса в материалах нет. Демо-значения в `src/data/prices.ts`.
- **Контакты и реквизиты** — заглушки в `src/config/site.ts`.
- **Способы оплаты** — не указаны, на `/delivery` нейтральный текст с пометкой.
- **Отзывы** — реальных нет, три демо-карточки с флагом `demo: true`.
- **Версаль / Верона** — по решению заказчика разведены как две модели.
- **Честер** — ТВ до 65″ (описание) против 75″ (инфографика); взято из описания.
- **Мощность** — 750/1500 Вт (описания) против 900/1500 Вт (одна инфографика).
- **Модерн, Дублин Премиум** — описаний нет, характеристики собраны по фото.
- **Вес изделий** — не указан, для доставки оценивается по габаритам.
- **Фотографии цеха** — отсутствуют, стоят честные заглушки `PlaceholderImage`.

---

## 9. История правок заказчика

| Итерация | Что просили                                                                                           | Итог                                                                                                                 |
| -------- | ----------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| 1        | Разработать сайт по ТЗ                                                                                | 18 товаров, все сценарии, 24 теста                                                                                   |
| 2        | Выложить на Netlify                                                                                   | Репозиторий, сайт, автодеплой через webhook                                                                          |
| 3        | Проверить работу                                                                                      | 24 теста на боевом адресе                                                                                            |
| 4        | Неразрывные пробелы, кликабельная карточка, белая галочка, стрелка сортировки, сильнее пламя          | Все пять; +2 теста                                                                                                   |
| 5        | Две ТК с выбором, структура «Оплаты и доставки», FAQ ниже формы, современный дизайн блоков о компании | Все четыре + карусель баннеров как первый экран, обложки категорий, карандашные иконки                               |
| 6        | Дублирование шагов на `/delivery`                                                                     | Одна цепочка из 4 шагов                                                                                              |
| 7        | Блок «Оплата» прижать влево                                                                           | Убрана колоночная сетка                                                                                              |
| 8        | Надписи на главных фото карточек                                                                      | `CARDS` в `make-covers.mjs`                                                                                          |
| 9        | Главное фото не помещается на экран                                                                   | Ширина считается из высоты окна                                                                                      |
| 10       | Перенести на Vercel                                                                                   | Новый адрес, заголовки в `next.config.ts`, Netlify убран; 32 теста на боевом адресе                                  |
| 11       | Иконки «как нарисованные карандашом»                                                                  | Все 48 иконок пересобраны через rough.js; lucide больше не в браузере                                                |
| 12       | Оформление шагов с главной — на «О нас» и «Доставку»                                                  | Общий компонент `StepsPlate`; те же шаги больше не свёрстаны трижды                                                  |
| 13       | Карусель видов каминов на «О нас», светлый первый экран                                               | `CategoryCarousel`; тёмной осталась только плашка с этапами                                                          |
| 14       | Дизайн главной, 5 правок по скриншотам                                                                | Новый первый экран, фотоплитки категорий, прокрутка фильтров, ритм отступов, обложка и текст акции                   |
| 15       | Качество фото товара, открытие фото на весь экран                                                     | Убрано растягивание мелких исходников, чистая карточка «Дублин Премиум», просмотрщик `Lightbox`                      |
| 16       | Полноценный визуальный редизайн                                                                       | Пара шрифтов, тёплая палитра, малые радиусы, editorial-композиции вместо сеток одинаковых карточек                   |
| 17       | Улучшенные фото от заказчика                                                                          | Механизм `incoming/products`; заменены главные кадры четырёх моделей, сняты обрезки для «Честера» и «Дублин Премиум» |
| 18       | «Фото не заменились»                                                                                  | Файлы были новыми, мешал недельный кэш на `/images/*` — заменён на 5 минут со `stale-while-revalidate`               |
| 19       | Кадры очага и фактуры в хорошем качестве                                                              | Подмена через `incoming/content`; запрет увеличения во всех производных изображениях                                 |
| 20       | Четыре правки по скриншотам                                                                           | Полоса перед подвалом, пометки перевозчиков, плавность карусели, запас под липкую шапку у якорей                     |

---

## 10. Команды

```bash
npm run dev            # разработка
npm run build          # production-сборка
npm run lint           # ESLint
npm run typecheck      # TypeScript
npm run format         # Prettier
npm run test:e2e       # 38 тестов, сами поднимут сборку

# тесты по развёрнутому адресу
E2E_BASE_URL=https://studiya-kaminov.vercel.app npm run test:e2e
```

> В этой среде у headless-браузеров периодически пропадают системные библиотеки
> (`libglib-2.0.so.0` и др.). Лечится:
> `sudo apt-get update -qq && sudo apt-get install -y libglib2.0-0 libnss3 libnspr4 libdbus-1-3 libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2 libxkbcommon0 libatspi2.0-0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 libgbm1 libpango-1.0-0 libcairo2 libasound2t64`

---

## 11. Что дальше

1. Получить от заказчика данные из раздела 8 — прежде всего цены и контакты.
2. Подключить боевые API СДЭК и «Деловых Линий», SMTP — переменные в `.env.example`.
3. Заменить заглушки фотографий производства.
4. Удалить мёртвый сайт на Netlify, чтобы не расходились две копии.
